# Default localhost API URL (leave blank to dynamically match current browser IP/domain)
# VITE_API_BASE_URL=http://localhost:5001/api

# For direct local testing using explicit LAN IP if needed:
# VITE_API_BASE_URL=http://192.168.1.117:5001/api
